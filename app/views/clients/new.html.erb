<h1>New Client</h1>

<%= form_with(model: @client, url: clients_path, local: true) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :email %>
  <%= form.email_field :email %>

  <%= form.submit 'Create Client' %>
<% end %>

<%= link_to 'Back', root_path %>